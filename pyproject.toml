[project]
name = "encryptedterminalchat"
version = "1.0.0"
description = "A secure, end-to-end encrypted terminal chat network featuring ephemeral keys and native ANSI rendering."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "cryptography>=50.0.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
